<?xml version="1.0" encoding="utf-8"?>
<configuration>

<!--
    Paths are still relative to the test project...
    TODO: This could be better to capture a ConfigPath (or BasePath) for each configuration entry
    that defaults to TestProjectFolder and enables a Resolve by the entry.Config/BasePath.
-->
    <appSettings>
        <add key="TestHelper/PreLoadedAssemblies" value="LocalTestHelper" />
    </appSettings>

</configuration>
